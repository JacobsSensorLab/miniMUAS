[package]
name = "muas-rc"
description = "1-to-many RC over NDN: USB controller on the GCS driving the fleet via Spark streams."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
tracing = { workspace = true }
